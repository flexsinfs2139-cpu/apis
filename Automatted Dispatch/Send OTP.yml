info:
  name: Send OTP
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/send-otp"
  body:
    type: json
    data: |-
      {
        "mobile": "91886-073-3786"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
