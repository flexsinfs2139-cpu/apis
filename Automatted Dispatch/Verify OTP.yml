info:
  name: Verify OTP
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/verify-otp"
  body:
    type: json
    data: |-
      {
        "mobile": "91886-073-3786",
        "code": "164380"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
