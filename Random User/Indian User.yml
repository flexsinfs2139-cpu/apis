info:
  name: Indian User
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/?nat=in"
  params:
    - name: nat
      value: in
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
