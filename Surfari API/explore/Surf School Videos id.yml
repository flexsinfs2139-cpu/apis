info:
  name: Surf School Videos id
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/explore/surf-school/videos/1"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
