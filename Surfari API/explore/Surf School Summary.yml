info:
  name: Surf School Summary
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/explore/surf-school/summary"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
