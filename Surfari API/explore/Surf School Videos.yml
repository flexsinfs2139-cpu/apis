info:
  name: Surf School Videos
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/explore/surf-school/videos"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
