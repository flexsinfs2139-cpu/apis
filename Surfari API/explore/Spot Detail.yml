info:
  name: Spot Detail
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_URL}}/explore/spots/{{SPOT_ID}}/detail"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
