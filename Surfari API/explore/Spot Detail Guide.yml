info:
  name: Spot Detail Guide
  type: http
  seq: 11

http:
  method: GET
  url: "{{BASE_URL}}/explore/spots/{{SPOT_ID}}/guide"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
