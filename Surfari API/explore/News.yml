info:
  name: News
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/explore/news"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
