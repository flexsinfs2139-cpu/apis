info:
  name: admin login
  type: http
  seq: 2

http:
  method: GET
  url: https://admin.surfari.flexsin.org/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
