info:
  name: resend-otp
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/auth/resend-otp"
  headers:
    - name: Authorization
      value: Bearer {{TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
