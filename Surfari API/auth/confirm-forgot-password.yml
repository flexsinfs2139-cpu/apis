info:
  name: confirm-forgot-password
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/auth/confirm-forgot-password"
  body:
    type: json
    data: |-
      {
        "forget_pass_token": "{{FORGET_PASS_TOKEN}}",
        "otp": "243655",
        "new_password": "{{PASSWORD}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
