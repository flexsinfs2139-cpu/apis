info:
  name: forgot-password
  type: http
  seq: 7

http:
  method: POST
  url: "{{BASE_URL}}/auth/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "surfarie@yopmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
