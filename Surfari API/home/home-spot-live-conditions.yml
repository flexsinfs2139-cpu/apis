info:
  name: home-spot-live-conditions
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/home/live-status"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
