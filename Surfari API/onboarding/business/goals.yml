info:
  name: goals
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BASE_URL}}/onboarding/business/goals"
  headers:
    - name: Authorization
      value: Bearer {{TOKEN}}
  body:
    type: json
    data: |-
      {
        "goals": ["getMoreBookingsAndLeads", "buildMyBrandOnSurfari"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
