info:
  name: spot-uvIndex-matrices
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/spot-uvIndex-matrices?lat={{LATITUDE}}&lng={{LONGITUDE}}"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  params:
    - name: lat
      value: "{{LATITUDE}}"
      type: query
    - name: lng
      value: "{{LONGITUDE}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
